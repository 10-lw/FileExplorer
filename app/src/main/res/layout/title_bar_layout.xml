<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/actionBarSize"
    android:background="@color/colorPrimaryDark"
    android:orientation="horizontal">

    <Button
        android:id="@+id/title_bar_back"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@null"
        android:drawableLeft="@mipmap/back"
        android:drawableStart="@mipmap/back"
        android:gravity="center"
        android:text="@string/back"
        android:textColor="@android:color/white" />

    <RadioGroup
        android:id="@+id/title_bar_radio_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/single_bg"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="2dp">

        <RadioButton
            android:id="@+id/radio_button_apps"
            android:layout_width="@dimen/title_bar_button_width"
            android:layout_height="@dimen/title_bar_button_height"
            android:background="@drawable/radiobutton_bg"
            android:button="@null"
            android:checked="true"
            android:gravity="center"
            android:text="@string/apps"
            android:textColor="@drawable/rediobutton_text_color" />

        <RadioButton
            android:id="@+id/radio_button_files"
            android:layout_width="@dimen/title_bar_button_width"
            android:layout_height="@dimen/title_bar_button_height"
            android:background="@drawable/radiobutton_bg"
            android:button="@null"
            android:gravity="center"
            android:text="@string/files"
            android:textColor="@drawable/rediobutton_text_color" />

    </RadioGroup>

</RelativeLayout>